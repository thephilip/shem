# Web UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a browser-based split-panel UI for starting agents and watching their streaming output, served from `priv/static/` by the existing Bandit server.

**Architecture:** Three static files (`alpine.min.js` vendored, `index.html`, `app.js`) served by `Plug.Static` from `priv/static/`. `HTTP.Router` gains a `GET /` fallback and moves MCP from `/` to `/mcp`. The Alpine.js component talks directly to the existing REST API and SSE streaming endpoint — no new backend code needed.

**Tech Stack:** Alpine.js v3 (vendored, ~15kb), Plug.Static, existing Bandit/Plug REST API

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Modify | `lib/shem/http/router.ex` | Add `Plug.Static`, `GET /` fallback, move MCP to `/mcp` |
| Modify | `lib/shem/mcp/router.ex` | Update hardcoded `/message` endpoint URL to `/mcp/message` |
| Modify | `test/shem/http/router_test.exs` | Update `POST /message` → `/mcp/message`; add `GET /` test |
| Create | `priv/static/alpine.min.js` | Vendored Alpine.js v3 — download once, commit |
| Create | `priv/static/index.html` | Single-page app shell, split-panel layout |
| Create | `priv/static/app.js` | Alpine component — all reactive state and API calls |

---

## Task 1: Routing change

**Files:**
- Modify: `lib/shem/http/router.ex`
- Modify: `lib/shem/mcp/router.ex`
- Modify: `test/shem/http/router_test.exs`

- [ ] **Step 1: Update the existing HTTP router test and add new assertions**

Replace the entire contents of `test/shem/http/router_test.exs` with:

```elixir
defmodule Shem.HTTP.RouterTest do
  use ExUnit.Case, async: false
  import Plug.Test
  import Plug.Conn

  alias Shem.HTTP.Router

  @opts Router.init([])

  test "GET /api/presets is forwarded to the REST router" do
    conn = conn(:get, "/api/presets") |> Router.call(@opts)
    assert conn.status == 200
    assert get_resp_header(conn, "content-type") |> hd() =~ "application/json"
    body = Jason.decode!(conn.resp_body)
    assert is_list(body)
  end

  test "POST /mcp/message with a JSON-RPC notification (no id) returns 204" do
    body = Jason.encode!(%{"jsonrpc" => "2.0", "method" => "notifications/initialized", "params" => %{}})
    conn =
      conn(:post, "/mcp/message", body)
      |> put_req_header("content-type", "application/json")
      |> Router.call(@opts)
    assert conn.status == 204
  end

  test "GET /unknown-path-xyz returns 404" do
    conn = conn(:get, "/unknown-path-xyz") |> Router.call(@opts)
    assert conn.status == 404
  end
end
```

- [ ] **Step 2: Run to confirm `POST /mcp/message` test fails**

```bash
mix test test/shem/http/router_test.exs 2>&1 | tail -15
```

Expected: `POST /mcp/message` returns 404 (MCP still at `/`).

- [ ] **Step 3: Update `HTTP.Router`**

Replace the entire contents of `lib/shem/http/router.ex` with:

```elixir
defmodule Shem.HTTP.Router do
  use Plug.Router

  plug Plug.Static,
    at: "/",
    from: :shem,
    gzip: false,
    only: ~w(alpine.min.js app.js)

  plug :match
  plug :dispatch

  forward "/api", to: Shem.REST.Router
  forward "/mcp", to: Shem.MCP.Router

  get "/" do
    path = Application.app_dir(:shem, "priv/static/index.html")
    send_file(conn, 200, path)
  end

  match _ do
    conn
    |> put_resp_content_type("application/json")
    |> send_resp(404, Jason.encode!(%{error: "not found"}))
  end
end
```

- [ ] **Step 4: Update the MCP router's hardcoded endpoint URL**

In `lib/shem/mcp/router.ex`, find line 18 (inside `get "/sse"`):

```elixir
    endpoint_url = "http://localhost:#{port}/message?sessionId=#{session_id}"
```

Change it to:

```elixir
    endpoint_url = "http://localhost:#{port}/mcp/message?sessionId=#{session_id}"
```

- [ ] **Step 5: Run the updated tests**

```bash
mix test test/shem/http/router_test.exs
```

Expected: all 3 tests pass. (`GET /` test will be added in Task 2 once `index.html` exists.)

- [ ] **Step 6: Run the full test suite**

```bash
mix test
```

Expected: all existing tests pass. Count unchanged (no new tests yet).

- [ ] **Step 7: Commit**

```bash
git add lib/shem/http/router.ex lib/shem/mcp/router.ex test/shem/http/router_test.exs
git commit -m "feat: HTTP router — serve static files at /, move MCP to /mcp"
```

---

## Task 2: Static files

**Files:**
- Create: `priv/static/alpine.min.js`
- Create: `priv/static/index.html`
- Create: `priv/static/app.js`
- Modify: `test/shem/http/router_test.exs`

- [ ] **Step 1: Create the `priv/static/` directory**

```bash
mkdir -p priv/static
```

- [ ] **Step 2: Vendor Alpine.js v3**

Download the Alpine.js v3 CDN build and save it locally. Go to https://alpinejs.dev/essentials/installation for the current version, or run:

```bash
curl -sL "https://cdn.jsdelivr.net/npm/alpinejs@3/dist/cdn.min.js" -o priv/static/alpine.min.js
```

Verify it downloaded (should be ~45–50kb minified):

```bash
wc -c priv/static/alpine.min.js
```

Expected: a number above 40000.

- [ ] **Step 3: Create `priv/static/app.js`**

```js
function shem() {
  return {
    preset: 'general',
    presets: ['general'],
    task: '',
    status: 'idle',
    output: '',
    errorMsg: '',
    agentId: null,
    eventSource: null,

    async init() {
      try {
        const res = await fetch('/api/presets');
        const data = await res.json();
        this.presets = data.map(p => p.name);
        if (this.presets.length > 0 && !this.presets.includes(this.preset)) {
          this.preset = this.presets[0];
        }
      } catch (_) {
        // keep default ['general']
      }
    },

    async run() {
      this.status = 'running';
      this.output = '';
      this.errorMsg = '';

      let res;
      try {
        res = await fetch('/api/agents', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ preset: this.preset, task: this.task })
        });
      } catch (e) {
        this.status = 'error';
        this.errorMsg = 'Network error: ' + e.message;
        return;
      }

      if (!res.ok) {
        const data = await res.json().catch(() => ({}));
        this.status = 'error';
        this.errorMsg = data.error || ('HTTP ' + res.status);
        return;
      }

      const { agent_id } = await res.json();
      this.agentId = agent_id;

      const es = new EventSource('/api/agents/' + agent_id + '/stream');
      this.eventSource = es;

      es.onmessage = (e) => {
        const msg = JSON.parse(e.data);
        if (msg.type === 'chunk') {
          this.output += msg.content;
          this.$nextTick(() => {
            const el = this.$refs.outputBody;
            if (el) el.scrollTop = el.scrollHeight;
          });
        } else if (msg.type === 'done') {
          this.status = msg.status === 'error' ? 'error' : 'done';
          if (msg.status === 'error') this.errorMsg = 'Agent finished with an error.';
          es.close();
          this.eventSource = null;
        }
      };

      es.onerror = () => {
        if (this.status === 'running') {
          this.status = 'error';
          this.errorMsg = 'Connection lost.';
        }
        es.close();
        this.eventSource = null;
      };
    },

    async stop() {
      if (this.eventSource) {
        this.eventSource.close();
        this.eventSource = null;
      }
      if (this.agentId) {
        await fetch('/api/agents/' + this.agentId, { method: 'DELETE' }).catch(() => {});
      }
      this.status = 'idle';
      this.agentId = null;
    },

    reset() {
      this.output = '';
      this.errorMsg = '';
      this.task = '';
      this.status = 'idle';
      this.agentId = null;
    }
  };
}
```

- [ ] **Step 4: Create `priv/static/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shem</title>
  <script src="/alpine.min.js" defer></script>
  <script src="/app.js" defer></script>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg:      #0f0f1a;
      --surface: #1a1a2e;
      --border:  #2a2a4a;
      --accent:  #7c6af7;
      --text:    #e0e0f0;
      --muted:   #666688;
      --green:   #8be9a0;
      --red:     #ff6b6b;
    }

    html, body {
      height: 100%;
      background: var(--bg);
      color: var(--text);
      font-family: 'JetBrains Mono', 'Fira Code', monospace;
      font-size: 14px;
    }

    .layout { display: flex; height: 100vh; }

    .sidebar {
      width: 280px;
      flex-shrink: 0;
      background: var(--surface);
      border-right: 1px solid var(--border);
      padding: 24px 20px;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .logo {
      font-size: 18px;
      font-weight: 700;
      color: var(--accent);
      letter-spacing: 2px;
      text-transform: uppercase;
      padding-bottom: 16px;
      border-bottom: 1px solid var(--border);
    }

    .field { display: flex; flex-direction: column; gap: 6px; }

    .field label {
      font-size: 11px;
      color: var(--muted);
      letter-spacing: 1px;
      text-transform: uppercase;
    }

    select, textarea {
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 4px;
      color: var(--text);
      font-family: inherit;
      font-size: 13px;
      padding: 8px 10px;
      outline: none;
      transition: border-color 0.15s;
    }
    select:focus, textarea:focus { border-color: var(--accent); }
    textarea { resize: vertical; min-height: 120px; line-height: 1.5; }

    .btn {
      padding: 10px;
      border-radius: 4px;
      border: none;
      font-family: inherit;
      font-size: 13px;
      cursor: pointer;
      transition: opacity 0.15s;
      width: 100%;
    }
    .btn:disabled { opacity: 0.4; cursor: not-allowed; }
    .btn-primary { background: var(--accent); color: #fff; }
    .btn-primary:not(:disabled):hover { opacity: 0.85; }
    .btn-stop { background: transparent; border: 1px solid var(--red); color: var(--red); }
    .btn-stop:hover { background: var(--red); color: #fff; }
    .btn-reset { background: transparent; border: 1px solid var(--border); color: var(--muted); }
    .btn-reset:hover { border-color: var(--accent); color: var(--accent); }

    .output { flex: 1; display: flex; flex-direction: column; overflow: hidden; }

    .output-header {
      padding: 12px 20px;
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      gap: 12px;
      background: var(--surface);
      flex-shrink: 0;
    }

    .task-label {
      color: var(--muted);
      font-size: 12px;
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .badge {
      font-size: 10px;
      letter-spacing: 1px;
      text-transform: uppercase;
      padding: 2px 8px;
      border-radius: 3px;
    }
    .badge-running { background: var(--accent); color: #fff; }
    .badge-done    { background: #2a4a3a; color: var(--green); }
    .badge-error   { background: #4a2a2a; color: var(--red); }

    .output-body { flex: 1; overflow-y: auto; padding: 20px; line-height: 1.7; }

    .placeholder { color: var(--muted); font-style: italic; }

    .stream {
      white-space: pre-wrap;
      word-break: break-word;
      color: var(--text);
      font-family: inherit;
      font-size: 13px;
    }

    .cursor { color: var(--accent); animation: blink 1s step-end infinite; }
    @keyframes blink { 50% { opacity: 0; } }

    .error-bar {
      padding: 10px 20px;
      background: #4a2a2a;
      color: var(--red);
      font-size: 12px;
      flex-shrink: 0;
    }

    .action-bar {
      padding: 12px 20px;
      border-top: 1px solid var(--border);
      flex-shrink: 0;
    }
  </style>
</head>
<body x-data="shem()" x-init="init()">
  <div class="layout">
    <aside class="sidebar">
      <div class="logo">⬡ shem</div>

      <div class="field">
        <label>Preset</label>
        <select x-model="preset" :disabled="status === 'running'">
          <template x-for="p in presets" :key="p">
            <option :value="p" x-text="p"></option>
          </template>
        </select>
      </div>

      <div class="field">
        <label>Task</label>
        <textarea
          x-model="task"
          placeholder="What should the agent do?"
          :disabled="status === 'running'"></textarea>
      </div>

      <button class="btn btn-primary"
        @click="run()"
        :disabled="status === 'running' || task.trim() === ''"
        x-show="status !== 'running'">
        Run Agent
      </button>

      <button class="btn btn-stop"
        @click="stop()"
        x-show="status === 'running'">
        Stop
      </button>
    </aside>

    <main class="output">
      <div class="output-header" x-show="status !== 'idle'">
        <span class="task-label" x-text="task"></span>
        <span class="badge"
          :class="{
            'badge-running': status === 'running',
            'badge-done':    status === 'done',
            'badge-error':   status === 'error'
          }"
          x-text="status">
        </span>
      </div>

      <div class="output-body" x-ref="outputBody">
        <span class="placeholder" x-show="status === 'idle'">
          Start an agent to see output here.
        </span>
        <template x-if="status !== 'idle'">
          <span>
            <pre class="stream" x-text="output"></pre><span
              class="cursor"
              x-show="status === 'running'">▌</span>
          </span>
        </template>
      </div>

      <div class="error-bar" x-show="errorMsg !== ''" x-text="errorMsg"></div>

      <div class="action-bar" x-show="status === 'done' || status === 'error'">
        <button class="btn btn-reset" @click="reset()">New Task</button>
      </div>
    </main>
  </div>
</body>
</html>
```

- [ ] **Step 5: Add `GET /` test to the HTTP router test file**

In `test/shem/http/router_test.exs`, add this test after the existing three tests (before the final `end`):

```elixir
  test "GET / returns 200 with text/html" do
    conn = conn(:get, "/") |> Router.call(@opts)
    assert conn.status == 200
    assert get_resp_header(conn, "content-type") |> hd() =~ "text/html"
  end
```

- [ ] **Step 6: Run all tests**

```bash
mix test
```

Expected: all tests pass. Count increases by 1 (the new `GET /` test).

- [ ] **Step 7: Manual browser verification**

Start the server:

```bash
mix run --no-halt
```

Open http://localhost:4000 in a browser. Verify:
- [ ] Page loads with the split-panel layout (dark theme, "⬡ shem" logo)
- [ ] Preset dropdown is populated from the API
- [ ] Typing a task enables the Run Agent button
- [ ] Clicking Run Agent starts an agent and shows streaming output in the right panel
- [ ] Status badge shows "running" then "done"
- [ ] "New Task" button appears when done and resets the form

- [ ] **Step 8: Commit**

```bash
git add priv/static/alpine.min.js priv/static/index.html priv/static/app.js test/shem/http/router_test.exs
git commit -m "feat: Web UI — Alpine.js split-panel agent interface"
```
