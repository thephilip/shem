# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# Temporary files, for example, from tests.
/tmp/

# Mnesia data directories (created per node name)
Mnesia*
MnesiaCore*

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Stale demo run logs
demo-failure.out

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
shem-*.tar
.superpowers/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Local tooling — never publish
.claude/
OPENCODE_PROMPT.txt
run_qwen.sh
smoke_test.exs
scripts/smoke_llm.exs
shem-feature-ideas-scored.md

# local dev scaffolding — not for the public remote
PROJECT_NORTH_STAR.md
SESSION_HANDOFF.md
/docs/superpowers/
/docs/archive/

# Ponytail debt ledger — local tracking only
/PONYTAIL_DEBT.md
